#include <iostream>
using namespace std;

int main()
{
    int x, y;
    int sub;
    cout << "Type a number: ";
    cin >> x;
    cout << "Type another number: ";
    cin >> y;
    sub = x - y;
    cout << "Subtraction is: " << sub;

    return 0;
}